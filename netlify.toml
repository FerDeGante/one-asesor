[build]
  command   = "npm run build"
  publish   = "dist"

[dev]
  command    = "npm run dev:vite"   # arrancará Vite en dev
  functions  = "api"                # carpeta donde vive subscribe.js
  targetPort = 5173                 # puerto donde Vite escucha
